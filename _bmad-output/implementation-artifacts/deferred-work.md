# Deferred Work

## Deferred from: code review of 1-1-initialize-nextjs-project-with-core-tooling (2026-06-21)

- **No graceful SIGTERM/SIGINT shutdown handler** [`server.ts`] — `httpServer.close()` and `pool.end()` should be called on process signals. Deferred to Story 1.4 (Docker Compose stack) where the process lifecycle is defined.
- **SESSION_SECRET no runtime enforcement** [`.env.example`/`server.ts`] — No startup check that SESSION_SECRET is present and ≥32 chars. Deferred to Story 2.x auth implementation where the secret is first consumed.
- **VENDOR_SECRET / WATCHTOWER_HTTP_API_TOKEN no enforcement** [`.env.example`] — Blank values are not caught at startup. Deferred until vendor routes are implemented.
- **`global.__io` hot-reload reassignment** [`server.ts`] — `tsx watch` restarts the full process so no stale io instances occur in practice. Revisit if HMR is ever introduced at the server layer.
- **`getIO()` eager module-level call risk** [`src/server/socket/index.ts`] — If any Route Handler calls `getIO()` at import time (outside a request function), the module throws on load. Guidance: always call `getIO()` inside route handler functions. Enforce when first Route Handler is written (Story 2+).
- **Socket.io accepts any LAN connection — no auth** [`server.ts`] — Any device on the restaurant LAN can connect and receive broadcasts. Deferred to Epic 2 (authentication) where the Socket.io `connection` handler will enforce session token validation.
- **`drizzle.config.ts` `!` assertion on DATABASE_URL** [`drizzle.config.ts:8`] — Suppresses TS error but gives a cryptic failure if DATABASE_URL is unset during migration runs. Low operational impact; revisit when CI/CD pipeline is set up (Story 1.5).
- **No startup readiness timeout** [`server.ts`] — If `app.prepare()` stalls (e.g., webpack compilation hang), the process appears alive but the port is never bound. Add a timeout watchdog when operationalizing the Docker deployment (Story 1.4).
- **AC5 server-only build-time test abbreviated** [`src/server/db/index.ts`, `src/server/socket/index.ts`] — The `server-only` import-boundary enforcement was verified via `tsc --noEmit` only. A live test (create client component that imports server file, run `next build`, confirm error) should be done when the first Route Handler and client component coexist (Story 2.1 or 2.2).

## Deferred from: code review of 1-2-configure-design-token-system (2026-06-21)

- **Duplicate token values in `@layer base :root` and `@theme`** [`globals.css`] — Every hex value is declared twice. `@layer base` block added as Turbopack dev-mode workaround (tokens not emitted until a utility references them). In `next build`, `@theme` emits all tokens unconditionally. Verify with a production build and consolidate to `@theme` only if confirmed. Risk: color values can drift between the two blocks during maintenance.
- **`x-staff-role` header cast without runtime validation** [`layout.tsx:21`] — `as 'waiter' | 'owner' | 'kitchen' | null` is TypeScript-only. Unexpected middleware header values write arbitrary strings to `data-context`, silently breaking role-based styling. Add `includes()` whitelist guard in Story 2.2 when session middleware sets the header.
- **`[data-context]` on `<html>` excludes `<html>` itself from variant scope** [`globals.css:7-9`] — `&:is([data-context="waiter"] *)` requires the target element to be a descendant of the attribute holder. `<html>` is not a descendant of itself. No current issue (no styles target `<html>` directly), but document as constraint: never apply `waiter:`/`owner:`/`kitchen:` utilities to the `<html>` element itself.
- **Custom `--text-*` tokens lack `--{name}--line-height` companion variables** [`globals.css:107-113`] — Tailwind v4 expects `--text-display--line-height` alongside `--text-display` for computed line-heights. Without companions, utilities use Tailwind's default `calc(1em + 0.5rem)`. Specify line-heights when typography is formally defined in component stories.
- **Dev Notes `@custom-variant` example shows invalid curly-brace syntax** [`1-2-configure-design-token-system.md:164-167`] — Shows `{ [data-context="waiter"] & }` which PostCSS rejects. Correct form is `(&:is([data-context="waiter"] *))`. Debug log in the story has the correct form. Update the Dev Notes template if `create-story` propagates this pattern into future stories.

## Deferred from: code review of 1-3-establish-database-schema-and-migration-pipeline (2026-06-21)

- **Missing CHECK constraints for positive/range integers** [`src/server/db/schema.ts`] — `cover_count >= 1`, `quantity > 0`, `amount_paisa > 0`, `portion_count IS NULL OR portion_count > 0`, `port BETWEEN 1 AND 65535`. Application-layer Zod validation handles these per architecture; adding CHECKs to append-only tables is irreversible. Revisit when API Route Handlers are written (Story 3+).
- **`taxRatePercent` no range constraint** [`tenant_config`] — No `CHECK (tax_rate_percent BETWEEN 0 AND 10000)`. Deferred to Story 10.x tenant config management.
- **`migrate.sh` needs `pg_isready` health check** [`docker/scripts/migrate.sh`] — Without a readiness loop, the script fails immediately if PostgreSQL is still initializing. Docker Compose `depends_on` health check handles this in Story 1.4.
- **`TRUNCATE` bypasses BEFORE row-level triggers on append-only tables** [`src/server/db/migrations/0001_append_only_rules.sql`] — TRUNCATE requires TABLE privilege. Deferred to Story 1.4 where the app DB user is created with restricted privileges (no TRUNCATE).
- **`tenant_config.updatedAt` not auto-refreshed on UPDATE** [`src/server/db/schema.ts:205`] — No ON UPDATE trigger; application code must set `updatedAt` manually. Deferred to Story 10.x tenant config management.
- **No unique constraint on `(tenant_id, label)` in `tables` and `(tenant_id, name)` in `zones`** [`src/server/db/schema.ts`] — Duplicate labels/names possible. Deferred to Story 3.x zone/table CRUD where application-layer uniqueness is enforced.
- **`orderEvents.menuItemId` nullable without event-type enforcement** [`src/server/db/schema.ts:130`] — `ITEM_ADDED`/`ITEM_REMOVED` etc. can be inserted with `menuItemId = NULL`. Application-layer Zod validation handles this. Defer to when Route Handlers are written.
- **`order_sessions.closedAt` could be earlier than `openedAt`** [`src/server/db/schema.ts:115`] — No `CHECK` constraint. Application-layer validation handles this. Defer.
- **No `onDelete`/`onUpdate` referential action on any FK** [`src/server/db/schema.ts`] — All FKs default to NO ACTION. Intentional per architecture; cascade semantics designed when tenant offboarding (Story 10.x) is implemented.
- **`tenant_config.brandColor` unconstrained `text`** [`src/server/db/schema.ts:209`] — Any string accepted. Input validated at API layer (Story 10.x). If rendered directly into CSS, sanitize before use.
- **`db:generate` and `db:migrate` have no `--config` flag** [`package.json`] — Rely on implicit `drizzle.config.ts` discovery. Low risk with config at repo root. Specify `--config drizzle.config.ts` when project structure changes.
- **`compRecords.amountPaysa` no positivity constraint** [`src/server/db/schema.ts:161`] — Zero or negative comp amounts accepted. Application-layer Zod validation handles this.

## Deferred from: code review of 1-4-configure-docker-compose-stack-and-health-endpoint (2026-06-21)

- **`SESSION_SECRET` silently empty if unset** [`docker-compose.yml:15`] — `${SESSION_SECRET}` has no `:-fallback`. Docker Compose injects empty string if unset. App should validate at startup. Deferred to Epic 2 (auth implementation) where the secret is first consumed.
- **`WATCHTOWER_HTTP_API_TOKEN` can be empty — API unauthenticated** [`docker-compose.yml:68`] — Default `:-` fallback means Watchtower HTTP API starts with no token. Mitigated by `internal` Docker network isolation. Deferred; enforce when vendor ops Story 11.x configures Watchtower API access.
- **`pg_isready` healthcheck passes before WAL recovery completes on hard shutdown** [`docker-compose.yml:37`] — PostgreSQL accepts connections before WAL replay finishes after power loss. Migration or queries run during this window may block. Low probability for a low-write-volume LAN POS. Deferred; fix with `psql -c "SELECT 1"` healthcheck if startup failures are observed in production.
- **Watchtower-triggered restart drops active Socket.io sessions mid-service** [`docker-compose.yml:58`] — Watchtower restarts the app container immediately when a new image is available, severing all WebSocket connections. Socket.io clients will reconnect but in-flight order/payment operations may be lost. Mitigated operationally: push new image builds only after service hours. Deferred to Story 11.x vendor ops.
- **No SIGTERM handler in `server.ts`** [`server.ts`] — Pre-existing from Story 1.1. `docker stop` → SIGTERM → process exits immediately, dropping in-flight HTTP requests and Socket.io connections. Add `httpServer.close()` + `io.close()` + `pool.end()` on SIGTERM. Deferred; compound with Story 1.1 deferred item.
- **Migration race on crash-restart** [`docker/scripts/migrate.sh`] — If container crashes mid-migration, drizzle-kit's advisory lock may block on restart until PostgreSQL idle-timeout clears the dangling lock. Unlikely for a single-tenant POS. Deferred; add `lock_timeout` to DATABASE_URL if observed.

## Deferred from: code review of 1-4 and 1-5 (2026-08-21)

- **Proxy defines no `has`, `set`, or `ownKeys` traps** [`src/server/db/index.ts:46`] — `'execute' in db` returns false and `Object.keys(db)` returns `[]`, because those operations hit the empty Proxy target rather than the real drizzle instance. No current call site depends on either. Add the traps when one does.
- **No pool shutdown path** [`src/server/db/index.ts`] — the lazily cached pool is never closed. Compounds the pre-existing missing SIGTERM handler from Story 1.1; fix both together when the process lifecycle is addressed.
- **`WATCHTOWER_HTTP_API_TOKEN` may still be empty** [`docker-compose.yml:92`] — pre-existing from Story 1.4. Mitigated while the Watchtower API port stays unpublished; becomes live the moment it is exposed. `WATCHTOWER_HTTP_API_METRICS=true` is already set, so this is one published port away from mattering.

## Deferred from: code review of 3-2-implement-zone-filtered-table-grid-with-real-time-status (2026-09-06)

- **Socket.io connections are unauthenticated** [`server.ts:37-50`, `src/server/socket/events.ts:33`] — No `io.use()` handshake middleware. `cors: { origin: false }` does not stop a direct socket client, and `src/proxy.ts` cannot cover it because Socket.io claims `/socket.io/*` on the same `httpServer` before Next's handler runs. Any LAN device can subscribe to live table occupancy, session ids and item counts without a PIN. **ESCALATION: this was already deferred from Story 1.1 to Epic 2. Epic 2 has now shipped without it. The deferral target has elapsed — this needs an explicit owner and a story, not another deferral.**
- **`itemCount` counts event rows, not quantities** [`src/app/api/tables/route.ts:73`] — `orderEvents.quantity` exists and is never summed; one `ITEM_ADDED` with `quantity: 3` renders "1 item". Deferred to Epic 4, which defines add/remove item semantics.
- **Response body is cast, not parsed** [`src/components/pos/table-grid.tsx:78-86`] — `body` is implicitly `any` and `data` is asserted to `TableGridRow[]`; a 200 response with a different shape renders `undefined` labels. Cross-cutting: no fetch or socket boundary in the project validates with `zod` yet, though it is installed. Address as a project-wide boundary-validation pass.
- **A zone with zero tables never appears in the filter** [`src/components/pos/table-grid.tsx:127-136`] — Zones are derived by folding over table rows, and `/api/tables` inner-joins from `tables`, so there is no second source. Latent while every zone has tables; becomes live at Story 10.3 (zone/table configuration).
- **Tenant selection is unpinned and differs between page and API** [`src/app/page.tsx:24-27`, `src/app/api/tables/route.ts:30`] — Two independent unordered `limit(1)` picks. The single-tenant assumption (NFR-SC1) is a comment, not a schema constraint. Only reachable if a second `tenants` row is ever created.
- **`handlerRef` assigned in a passive effect** [`src/hooks/use-socket.ts:52-56`] — An event landing between commit and effect flush invokes the previous render's handler. Harmless now (the handler closes only over the stable `queryClient`), but load-bearing for Epic 4/5 subscribers that close over props or state. Revisit when the second socket subscriber is added.
- **PIN pad may overflow at 375px** [`src/app/login/page.tsx`, `src/components/pos/pin-pad.tsx`] — Fixed-width keys total 312px against roughly 231px available inside the card padding at that viewport, against a comment claiming the pad must survive a narrow viewport. Not observed in a browser — verify before treating as real.
- **Unused `eslint-disable` directive** [`src/server/socket/index.ts:5`] — The only lint warning in the project; file untouched by this change.

## Deferred from: 4-digit PIN change (2026-09-06)

- **No rate limiting or lockout on `POST /api/auth/login`** [`src/app/api/auth/login/route.ts`] — There is no attempt throttling, no per-device backoff, and no account lockout. Fixing PIN length at 4 (PRD FR38, amended 2026-09-06) reduces the keyspace to 10,000 combinations, so an unattended tablet on the restaurant LAN can be walked through the entire space. The existing constant-time loop over all staff protects against timing oracles but does nothing about volume. **This became materially more important with the 4-digit change and should be closed before the system handles real money.** Options: per-IP/per-device exponential backoff, a short lockout after N failures, or a delay that grows with recent failure count. Belongs with Epic 2's auth work or a dedicated hardening story.
- **Existing 5- and 6-digit PINs would be unenterable** [`src/server/db/seed.ts`, staff records] — The login schema now rejects anything but 4 digits, and the pad cannot enter a fifth. All seeded PINs are 4 digits so nothing is affected today, but any real deployment that already issued a longer PIN would lock that staff member out with "PIN must be 4 digits". Needs a migration or a re-issue step before this ships to a site with existing accounts. No such site exists yet.

## Deferred from: code review of 3-3-implement-open-table-session (2026-09-06)

- **Role and active-status changes do not invalidate live sessions** [`src/server/services/session.service.ts`, `src/server/db/schema.ts:86`] — `session.role` is denormalised onto `staff_sessions` at login by deliberate design (Story 2.3), and `validateSession` never re-joins `staff` nor checks `is_active`. A demoted or deactivated staff member keeps their old role for the idle window, and indefinitely while they keep tapping because `touchSession` slides it. **Story 3.3's method-scoped RBAC now relies on that denormalised role to authorize a write, which raises the stakes on this pre-existing gap.** Needs an owner: invalidate sessions on staff change, or re-check `staff` inside `validateSession`.
- **Cross-tenant table id is not rejected** [`src/app/api/tables/[tableId]/sessions/route.ts`, `src/app/tables/[tableId]/page.tsx`] — Story 3.3 Task 1 specified loading the table "scoped to the tenant"; the implementation filters on id alone and derives `tenantId` from the row. Unreachable under NFR-SC1 (single tenant per stack) and arguably safer than trusting the caller's tenant, but the deviation was not recorded at the time. Revisit if multi-tenancy is ever introduced.
- **FK violations inside the open-session transaction return a generic 500** [`src/app/api/tables/[tableId]/sessions/route.ts`] — The inner catch handles `23505` only. A `23503` (table or staff row deleted between the pre-check and the insert) becomes "Could not open the table. Try again." and the waiter retries indefinitely instead of receiving a 404. Not reachable through the HTTP API — no delete routes exist — only by direct database action.
- **A fourth-digit PIN typo is unrecoverable** [`src/components/pos/pin-pad.tsx`] — Auto-submit fires in the same handler as the keypress, so backspace is never reachable for the final digit and every mistype is a recorded failed authentication attempt. Accepted cost of the 2026-09-06 decision to remove the confirm tap. Becomes material if attempt-based lockout is added — see the rate-limiting entry above.

## Deferred from: code review of 3-7-implement-table-availability (2026-09-09)

- **Availability routes do not scope the table lookup by tenant** [`src/app/api/tables/[tableId]/out-of-service/route.ts`, `src/app/api/config/tables/[tableId]/return-to-service/route.ts`] — Both select the table by id alone and take `tenantId` from the row, matching `sessions/route.ts` and every route before them. Unreachable under NFR-SC1 (single tenant per stack). If it were reachable, a cross-tenant table id would be actionable and the immutable audit row would pair one tenant's table with another tenant's staff. Already logged from Story 3.3's review; re-confirmed across two more routes. Revisit if multi-tenancy is ever introduced.
- **A table with availability history can never be deleted** [`src/server/db/migrations/0004_cold_lifeguard.sql`, `0005_table_status_events_append_only.sql`] — `table_status_events.table_id` is `ON DELETE no action` and the append-only trigger blocks deleting the referencing rows, so any table that was ever taken out of service is permanently undeletable. The same trap already exists for the other audit tables, but this extends it to `tables` — and **Story 10.3 explicitly specifies deleting zones and tables**, so it will hit this. Needs a soft-delete column or a documented archival path before 10.3 is built.

## Deferred from: 3-8-implement-within-zone-table-merging (2026-09-09)

- **AC-9 (tickets name every table) cannot be satisfied yet** [Epic 5] - The AC constrains ticket generation, and no ticket generation exists in the codebase; Epic 5 is unbuilt. The data it needs is already there: every table on a session is in `order_session_tables` with `released_at IS NULL`, and `/api/tables` already assembles `groupTableLabels` from it for the grid. **Whoever builds the kitchen ticket must read the group, not `order_sessions.table_id`** - that column is only the primary table now, so a ticket keyed on it would name one table of three and send the runner to the wrong end of the restaurant. FR18 was amended for this on 2026-09-06.
- **A merge is capped at 12 tables by the request schema, not by anything real** [`src/app/api/tables/[tableId]/merge/route.ts`, `sessions/route.ts`] - `max(11)` additional tables is an arbitrary bound chosen to keep a malformed request from attaching the whole floor to one session. No zone has more than a handful of tables today. If a real party ever needs more, the number is in two zod schemas and nowhere else.
- **Un-merge does not move items** [`src/app/api/tables/[tableId]/unmerge/route.ts`] - Releasing a table leaves every item on the remaining session, because items are not attributed to a table (only to a session, and from Epic 4 optionally to a person). Correct for v1 and consistent with the PRD's split-by-person settlement, but it means "un-merge the table the second party sat at" does not separate their food. Retroactive item re-assignment is Growth tier (`prd.md:113`).

## Deferred from: 3-9-implement-counter-sale (2026-09-10)

- **AC-11 — a counter order's ticket must be labelled `COUNTER`** [Epic 5] - FR18 as amended requires every ticket to name every table in the session, *or* the label `COUNTER` where there is none. No ticket generation exists, so this cannot be built or tested in Story 3.9. Joins the FR18 merged-table entry already logged against Epic 5: **the ticket must read the session's attached tables, and fall back to `COUNTER` when there are none** — a blank destination line sends the runner nowhere.
- **`GET /api/sessions/:sessionId/history` will 403 for kitchen staff** [Epic 6, Epic 7.4] - Story 3.9 adds `{ prefix: '/api/sessions', roles: ['owner','waiter'] }`, which it must, because the writes beneath that prefix are order entry. But Epic 6 specifies the history endpoint as readable by *any* authenticated staff member, and **a longer prefix cannot carve out the exemption**: the session id sits in the middle of the path, and a dynamic segment defeats prefix specialisation — the same limitation already documented against `/api/tables`. Whoever builds the history or audit view should put it under its own namespace (`/api/audit/...`) rather than trying to except it out of `/api/sessions`. Cheap to decide now, expensive after the endpoint ships.

## Deferred from: code review of 3-8-implement-within-zone-table-merging (2026-09-10)

- **New merge/un-merge routes perform no tenant scoping** [`src/app/api/tables/[tableId]/merge/route.ts`, `unmerge/route.ts`] - Neither reads a tenant, and `staffId` is taken raw from `x-staff-id` with no check that the staff row belongs to the table's tenant. Un-merge has no zone check either, so it is fully unscoped. Unreachable under NFR-SC1 (single tenant per stack) and already logged from Stories 3.3 and 3.7, now extended to two more routes. In merge's case the zone-equality check blocks cross-tenant merges as a side effect — defence by accident, not by design. Revisit if multi-tenancy is ever introduced.
- **Server and client disagree on which statuses may be merged** [`merge/route.ts`, `sessions/route.ts`, `table-grid.tsx`] - The server rejects only `unavailable` targets; the client rejects anything that is not `open`. The server's remaining defence against an `occupied` target is the join-table fast path plus the unique index, both of which key on `order_session_tables` rather than `tables.status`. A table whose `status` is `occupied` with no un-released attachment row would pass every server check — `occupyTables`' predicate is only `ne(status, 'unavailable')`. Requires drift between two sources of truth that the derive-from-join design exists to prevent, so not currently reachable.
- **Group size is capped per request but never in total** [`merge/route.ts`, `sessions/route.ts`] - `max(11)` bounds a single call; `attachTables` has no cap and nothing counts the tables already on a session, so repeated merges can attach an unbounded number. The un-merge picker renders one button per member and the card spans two columns regardless. No zone has enough tables for this to matter today. The magic number 11 appears in two schemas with no shared constant.
- **`idx_order_session_tables_session_id` duplicates the composite primary key's leading column** [`src/server/db/schema.ts`] - The PK `(session_id, table_id)` already serves every `session_id`-prefixed lookup, so the extra btree is write cost for no read benefit. Removing it needs a migration, so it is not worth a standalone change; fold it into the next migration that touches this table (Story 3.9 adds one).
- **`groupTableLabels` is ordered by plain SQL collation** [`src/app/api/tables/route.ts`] - `.orderBy(asc(tables.label))` gives "Table 10" before "Table 2", while the client deliberately re-sorts with `localeCompare(..., { numeric: true })`. Only `.length` is consumed today so nothing is visibly wrong, but the field's doc comment presents the array as ordered and consumable — a future reader who renders it directly gets the wrong order.
- **`unavailableReason: null` is hardcoded at four emit sites** [`sessions/route.ts`, `merge/route.ts`, `unmerge/route.ts`, `sessions/close/route.ts`] - Task 6 said to pass the table's existing value. Benign today: migration 0006's `CHECK ((status = 'unavailable') = (unavailable_reason IS NOT NULL))` makes it impossible for an `open` or `occupied` table to carry a reason. Recorded because the safety now rests entirely on a constraint introduced by a different story — if that CHECK is ever relaxed, four sites start wiping reasons from remote caches with no signal.

## Deferred from: code review of 3-9-implement-counter-sale (2026-09-11)

- **The `abandoned` item check runs outside the close transaction** [`src/app/api/sessions/[sessionId]/close/route.ts`, and the same shape in the table-addressed close from Story 3.6] - The session lookup and the `ITEM_ADDED` count are separate reads before the transaction, and `closeSession` re-checks neither; it only asserts `closed_at IS NULL`. An item committing in that gap produces a session closed as `abandoned` while carrying items, with `abandoned` in the audit note. The comment asserts this "must always" hold "or the audit trail stops meaning anything", and the codebase's own rule elsewhere is that "the check has to hold the same lock as the write or it is decoration". Pre-existing from Story 3.6; fix both together when Epic 4 makes items reachable.
- **`openSession` does not dedupe `additionalTableIds`** [`src/server/services/table-session.service.ts`] - A repeated id violates the `(session_id, table_id)` primary key, raising 23505 under a constraint name that is NOT `idx_one_open_session_per_table`, so the named check misses it and the caller returns 500. Unreachable over HTTP because the open route dedupes case-insensitively before calling it, and the seat route does the same - but the service itself relies on every caller remembering.
- **No CHECK constraint pairs `kind` with `table_id`** [`src/server/db/schema.ts`] - Nothing prevents `kind = 'table'` with a null `table_id`, or `kind = 'counter'` alongside live `order_session_tables` rows. The schema comment argues `kind` must be an explicit column precisely because inferring it from join rows lets a race reclassify a session - but the column is now maintained by a single `if` inside `attachTables`, and read by the last-table guard and (later) Epic 9's revenue split. A missed write leaves an un-releasable or mis-attributed session and the database will not object. Fold a CHECK into the next migration touching this table.
- **`attachTables` picks the primary table by tap order, not label order** [`src/server/services/table-session.service.ts`] - `tableId: tableIds[0]` is the first element of the client's staging array, so seating at T7 then T3 makes T7 the primary: the order screen takes its zone and heading from T7 while the grid card leads with T3. `openSession` has the same ordering for `additionalTableIds`, so this is pre-existing rather than new - but Story 3.9 makes it reachable from the counter path too. Order by label, as the grid does.

## Deferred from: 4-2-implement-menu-browse-and-search (2026-09-11)

- **`stationTypeEnum` has no `pizza_kitchen`** [`src/server/db/schema.ts`] - FR9 routes items to Kitchen, Pizza Kitchen or Bar, and Story 4.2 adds a `production_destination` enum with all three. But `station_type` — which types printer and KDS station configuration — is still `['kitchen','bar']`. Story 4.2 deliberately did not touch it: it types a different thing. **Epic 5's ticket generation and Epic 10's station configuration must reconcile them**, or a pizza-kitchen KOT will have no station to print at. Decide there whether `station_type` gains the value or whether stations reference destinations by a different mechanism.
- **Quick Items — 8 per-waiter pinned items** [`ux-design-specification.md:420`] - Promised by the UX spec ("Configured from the waiter's own profile screen. Personal, not global"), with **no FR and no epic AC**. Needs a per-staff table and a profile surface, neither of which exists. Same "in the mockups, in no document" shape as counter sale; route it through a sprint change proposal rather than a developer's initiative.
- **Recents strip — last 5 items this waiter ordered** [`ux-design-specification.md:423`] - Same status: UX spec only, no FR, no AC. Needs order history queryable per staff member, which Epic 4.5 will make possible but nothing currently exposes.
- **Fuzzy match on kitchen aliases** [`ux-design-specification.md:415`] - The spec wants "GF" to surface "Grilled Fish" via fuzzy matching against item names **and kitchen aliases**. FR7 asks only for search "by item name or category", and Story 4.2 AC-3 specifies case-insensitive substring matching, which is what was built. An alias column does not exist. The kitchen-vocabulary point is real — `ux:43` notes ticket item names should match operational vocabulary, not customer-facing menu language — and that is worth its own requirement rather than being smuggled into search.

## Deferred from: 4-3-implement-seat-slot-management (2026-09-12)

- **The seat note must NOT appear on any guest-facing bill** [Epic 6 — Stories 6.2, 6.3, 6.4] - Story 4.3 adds `seat_slots.seat_note`, a short physical descriptor a waiter records because they cannot ask four strangers their names: "red shirt", "curly hair", "bald man". It renders on the seat chips and, per Epic 5, on the kitchen ticket — where it tells the runner who to hand the plate to. **A bill handed to a guest must show the seat LABEL only.** Teran's explicit decision, 2026-09-12. Whoever builds bill generation must select `seat_label` and never `seat_note`; a slip reading "Bald man - LKR 2,400" is not a mistake the customer forgives. Same reasoning keeps it out of any export or report.
- **The kitchen ticket SHOULD carry the seat note** [Epic 5 — Story 5.2] - The other half of the decision above. `Table 7 · Seat 2 (red shirt) · Grilled Fish` is the moment the note earns its keep: without it the runner has a seat number and four faces. FR18 already governs the ticket's table identifiers; the seat note is an addition to the per-item line, not to the header.

- **`order_events.round_number` does not exist, and Story 4.5 requires it** [`src/server/db/schema.ts`] - `epics.md:1398` specifies each `order_event` row carrying `session_id`, `seat_slot_id`, `menu_item_id`, `modifier_text`, `staff_id`, `round_number` and `submitted_at`. Story 4.3 adds `seat_slot_id`; `created_at` covers `submitted_at` and `notes` may cover `modifier_text`, but **`round_number` is absent entirely** — and both "Add More Items" (Story 4.4) and the KOT's `Table 7 · Round 2` header (Epic 5) depend on it. Whoever builds 4.4 or 4.5 adds the column; flagged here so it is a decision rather than a discovery mid-implementation.
- **`amountPaysa` is misspelled on two tables** [`src/server/db/schema.ts`] - `compRecords.amountPaysa` and `paymentRecords.amountPaysa`, both mapping the correctly-named column `amount_paisa`. Same typo Story 4.2 fixed on `menu_items.pricePaisa` and Story 4.3 fixes on `order_events.unitPricePaisa`. Left alone deliberately: nothing reads either key yet, and they belong to Epic 6 (payments) and Epic 7 (comps), which should rename them as they start writing to them rather than having a menu story reach into their tables.
- **Three chip rows now exist with no shared abstraction** [`zone-chip-bar.tsx`, `menu-category-chips.tsx`, `seat-chip-row.tsx`] - Each is a horizontally scrollable row of `h-touch-waiter` pills with an active state, and each has its own copy of the class list. Extraction was deliberately NOT done at the third instance: the three differ in what they carry (open counts, an icon, a delete affordance) and a premature shared component would be parameterised into uselessness. Worth revisiting if a fourth appears, or if the active-state styling drifts between them again — it already did once, when the AA contrast fix had to be applied in two places.

## Deferred from: code review of 4-3-implement-seat-slot-management (2026-09-12)

- **The seat item-count guard does not filter `event_type`** [`src/server/services/table-session.service.ts`, `deleteSeat`] - The guard counts every `order_events` row with `seat_slot_id = seatId`, regardless of type. Correct today, because `ITEM_ADDED` is the only type written with a seat. Once Story 4.5 writes `ITEM_VOIDED` or `ITEM_REMOVED` against a seat, a seat whose items were ALL voided still refuses deletion — which is right, since those append-only rows would still be orphaned — but the message "This seat has 3 items on it" will overstate what is actually on the seat. Story 4.5 should split the guard's count (any referencing row) from the message's count (live items).
- **A 23503 between the item count and the DELETE surfaces as a 500** [`src/app/api/sessions/[sessionId]/seats/[seatId]/route.ts`] - The count and the delete run in one transaction under a `FOR UPDATE` on the SESSION, not on the seat, so an item written to that seat by another device in the gap makes the FK refuse the delete. The route catches `SeatHasItemsError` but not the raw foreign-key violation, so the waiter gets "Could not remove the seat" instead of the actionable message. Unreachable until Story 4.4 can write items; add the `isForeignKeyViolation` catch when it can.
- **No route under `/api/sessions` is tenant-scoped** [every route handler] - `seat_slots` carries no `tenant_id` and no seat query joins to `order_sessions.tenant_id`. A valid staff session plus a session UUID from another tenant reads and writes that tenant's data. Harmless on a single-restaurant deployment, which is what this is — recorded because the seat note is a physical description of a named guest, so it is the first column where a cross-tenant read would be a privacy incident rather than a data leak. Belongs with whatever story makes this multi-tenant for real.
- **Two devices editing one seat note is a silent last-write-wins** [`src/app/api/sessions/[sessionId]/seats/[seatId]/route.ts`, PATCH] - The PATCH sends only the new value; there is no version or if-match predicate, and the editor's draft is seeded when the panel opens. Two waiters editing the same seat means the second Save silently discards the first. Low value to fix on a two-tablet floor; revisit if the note ever becomes something more than a memory aid.
- **No maximum number of seats per session** [`src/app/api/sessions/[sessionId]/seats/route.ts`] - Nothing caps seat creation. `nextSeatLabel` full-scans the session's seats on every add, and the chip row becomes unusable long before that matters. Same unbounded shape as several other collections here; worth one shared decision rather than a cap invented per route.
- **`role="status"` on conditionally-mounted elements may announce nothing** [`src/components/pos/order-screen.tsx`, and the pre-existing `notice` beside it] - The live region and its text are created in the same commit, so several screen readers announce neither. Pre-existing pattern across the POS screens; fix it once, everywhere, rather than in the one component a review happened to look at.
- **The order page makes five serial `await db.select` round trips before first paint** [`src/app/orders/[sessionId]/page.tsx`] - Session, staff, tenant config, item count and now seats, each awaited in turn. Pre-existing (it was four), and the seats query was appended to the end of the chain — on the screen whose stated design rationale is that the party is present in the first frame. A single `Promise.all` would do it; not this story's regression, but this story made the chain longer.

## Deferred from: 4-4-implement-add-item-to-order-with-modifiers-and-seat-assignment (2026-09-16)

- **Structured modifier groups** [Epic 10 — Story 10.4, menu management] - `ux:427` describes modifier groups as chip selectors ("Spice: Mild / Medium / Hot") with required options highlighted above optional ones. Story 4.4 ships **free-text** modifiers, which is what FR8 asks for ("modifiers or special instructions") and what the epic's own ACs exemplify ("no ice", "extra spicy"). The structured version needs a `modifier_groups` / `modifier_options` model, a per-item association, and an owner-facing surface to configure it — none of which has an FR or an epic AC. Same shape as Quick Items and Recents: in the mockups, in no requirement document. Whoever builds menu management decides whether this is in scope; do not smuggle it into an order-entry story.
- **Per-waiter modifier shortcuts** [`ux:59-60`, needs an FR first] - "Pre-set modifier shortcuts per item category reduce modifier entry to one tap. Personalisation matters: each waiter pins their own most-used modifiers (not a global default)." The spec calls personalisation "the mechanism that drives genuine adoption over time", which makes it a product claim worth a requirement rather than a footnote. Needs per-staff storage, which does not exist — the same missing table Quick Items and Recents need. Route all three through one sprint change proposal.
- **A staged round lives on one device only** [`src/components/pos/use-staged-round.ts`, Story 4.4] - AC-5 forbids a database write before submission, so Story 4.4 mirrors the staged round to `localStorage` to survive a reload. That mirror is per device by design: two tablets restoring the same round would double the order. The consequence is that a tablet that dies mid-round loses it, and the waiter re-enters from memory. Acceptable while submission is seconds away from staging; revisit if Epic 12's cloud relay ever makes a server-side draft cheap and unambiguous.

## Deferred from: order screen redesign (2026-09-16, Teran's mockup)

- **Service charge is not tax** [Epic 6 — billing] - The mockup's summary shows "Service 10%". The schema has only `tenant_config.tax_rate_percent` (default 0); there is no service-charge column. The order screen shows a tax row only when a rate is configured and shows no service line at all. A percentage added to a guest's total needs a column, a rule for whether it applies to comps, and a place on the bill — Epic 6 decides all three.
- **"Bill & split" in the order bar** [Epic 6 — Stories 6.1–6.4] - In the mockup's bottom bar, deliberately not rendered: the strip draws no button without a handler, and there is no billing yet. The strip already has `onGenerateBill`; wire it when 6.2 lands.
- **"Hold" in the order bar** [needs a requirement] - In the mockup, in no FR or epic AC. Plausibly "keep this round staged but don't send it yet" — which is what an unsent round already is — or "fire later" (course timing), which is a real kitchen feature with ticket implications. Ask before building.
- **"Send N to Kitchen"** [Story 4.5] - The bar's primary action. The strip's copy already reads `Send 3 to Kitchen & Bar` from the staged lines; 4.5 passes `onSubmitOrder` and the button appears.
- **Menu photographs dropped from the card** [`menu-item-card.tsx`] - The 2026-09-12 card showed meal images at Teran's request; the 2026-09-16 mockup is text-only for density. `image_url` is still stored and still returned by `/api/menu`; restoring photos is a change to the card alone. `menuCategoryIcon` in `src/lib/design.ts` is now unused and kept for that reason.

## Deferred from: code review of 4-4-implement-add-item-to-order-with-modifiers-and-seat-assignment (2026-09-16)

- **Two tabs on one device: last writer wins** [`src/components/pos/use-staged-round.ts`] - PARTLY RESOLVED in the 4.4 review: the store now listens for `storage` events, so both tabs SHOW the same round and a change in one appears in the other. Two taps landing in two tabs within the same instant can still overwrite each other, because each write replaces the whole array. A per-line merge would close it; not worth it until two tabs on one tablet is observed in practice.
- **Below the `md` breakpoint the order screen's columns do not scroll independently** [`src/components/pos/order-screen.tsx`] - The root is pinned to the viewport only from `md` up. On a phone or narrow portrait window the document scrolls, and "Close table" ends up below the whole menu and the whole order column — the regression Story 4.2's preserve list names. Tablets in landscape are the target; a phone layout is its own design decision.
- **`display: contents` on the seat group** [`src/components/pos/seat-selector.tsx`] - Removes the element's box, and in some browser/screen-reader pairs its group role and label with it. Pre-existing from the 4.3 chip row. Fix by rendering the grid on the group element itself.
- **No per-session authorisation on `GET /api/sessions/:id/orders`** [`src/app/api/sessions/[sessionId]/orders/route.ts`] - Any authenticated waiter can read any session's history, including seat notes. The same is true of every `/api/sessions` route; recorded here because this one returns physical descriptions of guests.
- **Stale `cdrms:staged:*` keys from sessions that were never closed on this device** [`src/components/pos/use-staged-round.ts`] - Closing a table now clears its own key, but a table closed on ANOTHER tablet leaves this one's key behind. A prune on load (drop keys for sessions not in the open list) would bound it.
- **`slate-400` text remains on other screens** [`pin-pad.tsx`, `table-grid.tsx`, `context-header.tsx`, input placeholders] - 2.56:1 on white. The 4.4 review removed it from every meaningful text on the order screen; the PIN pad hints, a floor-grid glyph and the header chevrons predate it and were left alone. One sweep, once, rather than piecemeal.

## Deferred from: seats-only-in-Tables decision (2026-09-16, Teran)

- **Orders outside the Tables zone cannot be split by seat at billing** [Epic 6 — Story 6.4] - Bean Bags, Sun Beds, Rooftop and counter sales now hide the seat section, and every item lands on Seat 1. Per-seat billing (6.4) therefore has nothing to split there; consolidated billing (6.2) and the mid-settlement person split (6.3) still apply. Whoever builds 6.4 should hide the per-seat option for those zones rather than offer a split with one seat in it.
- **The seat rule is keyed on the zone NAME** [`src/lib/design.ts`, `zoneUsesSeats`] - Renaming the "Tables" zone turns seats off there, silently. Acceptable while zones are seeded, not owner-edited; when Epic 10's zone configuration (Story 10.3) lands, make it a per-zone `uses_seats` column instead.

## Deferred from: 4-5-implement-order-submission-and-multi-destination-routing (2026-09-16)

- **sprint-change-proposal-2026-08-21 was only partly applied** [`_bmad-output/planning-artifacts/epics.md`] - Its Changes E1 (new Story 5.0, durable print queue), E2 (Story 5.2 rewritten to enqueue instead of printing synchronously) and E4 (Story 4.5 idempotency) never reached `epics.md`. `sprint-status.yaml` has `5-0-implement-durable-print-queue`, but no story text exists for it, and 5.2 still describes the synchronous TCP call the proposal replaced. Story 4.5 carries E4 itself as AC-8. **Run `bmad-correct-course` before Epic 5** to apply E1 and E2, and to correct Change A5's idempotency schema: `order_sessions.client_order_uuid UNIQUE` would make every round after the first collide, so the key belongs on `order_rounds` (Story 4.5 Task 1).
- **The architecture document is behind the code on order submission** [`_bmad-output/planning-artifacts/architecture.md:862-883`] - It still describes `POST /api/orders/:orderId/submit` with an inline `print.service.printTickets()` TCP call. The route is `POST /api/sessions/:sessionId/orders` (the epic's path), and printing moves to the Story 5.0 queue. Update both when correct-course runs.
- **There is no bar or pizza-kitchen staff role** [`staff_role` enum] - Story 4.5 lets the `kitchen` role join every production room (`kitchen`, `pizza_kitchen`, `bar`). A bar tablet signed in as a kitchen user will see kitchen tickets too. Epic 10's station configuration should decide whether stations get their own logins or a device-level station binding.

